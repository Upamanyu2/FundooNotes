import { SmallSearchPipe } from './small-search.pipe';

describe('SmallSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SmallSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
